{
"scripts": {
    "build": "tsc",
    "post-build": "node build/post-build.js",
    "start": "node build/index.js",
    "watch": "nodemon",
    "test": "mocha -t 20000 --reporter spec --exit 'build/**/*.spec.js'",
    "testonly": "mocha -t 20000 --exit 'build/**/*.spec.js'",
    "build-start": "yarn build && yarn start"
  }
}